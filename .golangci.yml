run:
  timeout: 2m

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - ineffassign
    - deadcode
    - unused

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
